<!DOCTYPE html>
<html>
<head>
  <title>Karate</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
 <%= render 'layouts/stylesheets' %>
</head>
<body>
   <div class="container">
     <%= render 'layouts/header' %>
      <section class="round">
         <%= image_tag("header.jpg", :alt => "Header Logo", :class => "head") %>
        <%= yield %>
      </section>
        <%= render 'layouts/footer' %>
    </div>    
</body>
</html>

